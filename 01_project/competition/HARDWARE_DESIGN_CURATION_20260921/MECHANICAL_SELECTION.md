# 机械数字样机精简选单

当前总装基准是 **R6H 后续集成总装**，不是早期WP03。原文件保留在 `20_engineering/SERVICE_STAR_CORE_INSTALLATION_R6H_20260920/native/SERVICE_STAR_SERVICE_R6H.SLDASM`，本次核验SHA256为 `30ccbcbcff047ef54a112b841cf4814646714b24141b822bacbca13e1a5e45a7`，127223字节。

受检总装包含机械臂、本体及后续水平MAIN、P60局部支承和热桥集成；计数 **15组、1153叶实例、1602实体**。R7 AUX/STOP尚未安装，不能混入上述计数。原件未删除、未改名、未回退；本轮仅创建 `20_engineering/SERVICE_STAR_HARDWARE_COMPACT_20260921/01_mechanical/` 内副本及派生清单。

## 最小应保留内容

| 内容 | 选取方式 | 用途与限制 |
|---|---|---|
| 当前原生机械总装及引用闭包 | 1顶层+15组+721唯一零件=737文件，99,490,779字节 | 从R6H成功冷开树和790项来源锁交集派生，不能按旧目录名排除 |
| 原生来源锁 | 原790项全部列入选择JSON并保留哈希；737项为当前依赖，53项local_only谱系 | 来源超集不是顶层实时引用数；不把53项从本机删除 |
| 当前局部STEP | R6H `NATIVE_ASSEMBLY_PLAN.parts` 的27项 | 是本轮新增/改型局部零件，不冒充完整整星STEP；完整星体和臂由原生装配提供 |
| 当前离线查看器/PNG | R6H水平安装实际STEP网格4面板 | 可离线读局部安装、支承、传热及推进预留。不是完整整星交互视图；颜色仅标记，不等于材料 |
| 当前装配BOM | 新派生 `CURRENT_ASSEMBLY_BOM_1153.csv` | 逐实例对应精简原生文件、位姿、实体数和来源；只在原生哈希一致时继承旧材料元数据，新/改件未定材料不零填 |
| 安装与材料说明 | R6H增量BOM、端口基准、安装说明、原审阅回执；R1材料计划/来源 | 旧1110行BOM只作为材料元数据来源，不作为当前总装BOM；项目自有候选材料库作为本地编辑辅助，不复制商业标准材料库 |
| 验证与精简工具 | 当前copy plan、源哈希、复制/重链接/冷开回执及本轮工具 | CAD直接打开不需要Python；本轮构建工具复用原工作区COM封装，尚不声称独立源码重建环境完整 |

737项来源：R1 677项、R4 28项、R6H 32项。目录名是谱系，R1/R4内仍包含当前机械臂和结构所需零件，**不能按R1/R4“旧版本”整目录删掉**。每项source、role、reason、include_type、deps详见 `MECHANICAL_SELECTION.json`。`public_candidate`只是候选，不表示已完成第三方再分发权清理。

## 可执行的安全复制与重链接路径

本机已有WP09 `system_completion/tools/portable_redirect.py` 与 `portable_cold_v2.py`：对副本执行 `ISldWorks.ReplaceReferencedDocument`，再异路径冷开。旧成功回执是705组件版本，不可以挪作R6H证明。旧GetPackAndGo绑定曾失败，当前采用已验证的闭文档引用替换方法，并明确**不是PackAndGo结果**。

本轮整理工具已经归档到本治理目录 `mechanical_curation_tools/curate_native.py`；以下是已执行流程，不是要求用户再次运行的步骤。脚本依赖来源库，另一次构建须明确新输出目录和回执位置，不能对已完成副本直接重跑：

1. `prepare`：锁790原件、从当前冷开树选737文件，生成确定性短文件名及1153实例身份/位姿映射。
2. `copy`：确认SolidWorks当前会话没有用户文档；复制零件和装配，零件保持原字节。仅对新目录内处于关闭状态的装配副本重链接，先15子装配后顶层；核对所有直接/递归原生依赖在新native内。原件前后790哈希不变。
3. `cold --location primary`：在精简目录只读冷开R6H总装，核对15组和1153实例身份、路径、固定状态与总变换；721个零件字节一致使原1602实体冷开证据保留。该步骤不声称重测了1602实体。
4. `move --location relocated`：只把自有副本native移到同一子目录的relocated_native，先验证两个绝对路径都在本次所有权范围内，原native副本目录在测试时缺席；不移动源工程。
5. `cold --location relocated`：以另一路径只读冷开，要求15/1153、零外部原生引用、变换/零件哈希不变；`move --location primary`恢复副本位置。两次冷开全部通过后才能声明该副本原生装配可迁移。

任何已有用户文档都保持原样，不自动关闭、保存或覆盖；内存/COM异常保存失败回执并停止。完整路径使用短哈希文件名，避免旧WP09在261字符路径的已知失败；正式支持范围以本轮实测路径长度与回执为准。

## 本次执行状态入口

**已完成**737个当前原生文件复制、16个装配副本闭文档重链接，以及原位置/异位置两次只读冷开。两次均为15组、1153叶实例、零打开错误/警告、零外部原生引用；721个零件逐字节一致，保留原1602实体实读信用。本轮没有重新测量1602实体。790个源文件哈希前后全部一致。副本已恢复native位置，最长原生路径174字符。

精简主目录只保留最终 `MECHANICAL_DELIVERY.json`、`SOURCE_TO_TARGET.csv`（784项=737原生+47辅助来源）、输入映射和设计文件。`NATIVE_COPY_RELINK.json`、两次冷开、移位、辅助复制等详细回执及过程日志归档在本治理目录 `mechanical_packaging_evidence/`；前两次审批超时后的原字节复制回执是过程历史，不是最终状态。

当前副本顶层SHA256为 `5381d95089c290472c91c271bef0094d92c5174fb64d8094278c727ab6d788c2`；与原件不同仅因副本引用重链接。原R6H顶层仍保持首段SHA256。最终裁决 `PASS_CURRENT_R6H_NATIVE_COPY_RELINK_AND_RELOCATED_COLD_OPEN`。

原生文件仍是固定姿态、含导入实体的数字设计候选；未补成原生参数化特征、运动配合或完整材料质量，也未获得制造/上电/飞行放行。此精简不涉及上层研究/控制软件。
