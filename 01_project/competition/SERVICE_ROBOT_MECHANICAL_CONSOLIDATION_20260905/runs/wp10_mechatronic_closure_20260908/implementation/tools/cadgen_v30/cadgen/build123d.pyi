# Type stub for the PEP 562 lazy re-export in build123d.py: type checkers and
# IDE completion see build123d's real names through the alias while the
# runtime module stays lazy. Transparent by design rule - never add or rename
# symbols here.
from build123d import *  # noqa: F401,F403
