# WP10 交付判定与研究起步建议独立审阅

2026-09-08，范围为 `tools/finalize_review.py` 的预备发布逻辑，以及 `ENGINEERING_RESEARCH_LOOP_ZH.md`。未执行发布脚本、仿真、训练或硬件；未修改被审阅文件。源文件哈希见 [JSON 回执](FINAL_SCOPE_REVIEW.json)。

**研究建议和整机交付分级总体恰当：完整样机机电设计仍未完成，可以准备并开展具有明确假设、版本和验证范围的模型研究；当前 873 构型不能据旧 PASS 直接获得真实硬件预测信用。** 不应把本审计角色的只读职责外推为所有后续数值研究都必须额外批准的新 Gate。建议文本不是执行回执，文中已明确本轮没有执行数值研究。

## 实质问题与处理

| 项目 | 状态 | 原因与修正 |
|---|---|---|
| 协议饱和值与真实驱动能力的区别 | 已修正 | 原“控制需求不能超过公开协议饱和值就视为可实现”存在逻辑歧义。重新读取的文档已改为“不能因控制需求未超过公开协议饱和值，就视为真实驱动可实现”，并要求力矩–速度–电压–温度、功率和延迟证据，问题已消除 |
| 材料惯性结果的最终发布绑定 | 收到正式回执后应补强 | 预备脚本目前只检查 `str(inertia.status).startswith('PASS')`。这个条件不能区分真实几何惯性提取、源文件准备或其他有限范围 PASS。材料代理尚在计算，因此本审阅不判断惯性结果已通过或失败。正式发布时应绑定它的 exact 状态、实际提取覆盖数量、质量/单位/参考系约定、有效值/负结果和所消费文件的 SHA，并把剩余未知单列；不应仅凭 PASS 前缀把“惯性补充已完成”写入交付范围 |

研究执行措辞的初步疑问经范围区分后不列为阻断。可以考虑在共同输入说明中补一句“本轮只提供建议与输入，未运行新数值研究；后续任务须明确模型版本及验证范围”，但这只是避免误读，不能新增无来源批准流程。

## 未发现过度限制或夸大的部分

- `complete_mechatronic_design_deliverable=false` 和 `complete_assembly_ready_prototype_design=false` 针对完整工程样机设计责任，未以飞行鉴定缺失替代内部设计开项；23 项保留为工作包计数，未包装成完成百分比。
- 原生 CAD 信用限于继承的 873 叶部件静态三态；没有把叶零件数当自由度数。逐 link 合并和完整运动树仍待闭合。
- 原质量覆盖 400 分解为 390 个逐实例数值与 10 个整臂共同覆盖，未将 B601 4.5 kg 平均分配至各 link。旧 R2、sim05 等模型身份被隔离。
- 29 条历史 raw SHA 漂移被保留；没有据此自动推翻历史结果，也没有宣称当前 checkout 已能逐位复现。
- 推荐从模型身份、守恒/退化验证、确定性规划/控制基线到 RL，且允许理论与假设模型研究同工程迭代。当前硬件有效性和合成算法研究的适用域分别陈述，没有让所有研究等到飞行鉴定。
- 半正弦接触载荷关系被限定为声明的模型内解析关系，未以它替代真实接触时窗或连接载荷资格化。
- SAFE/旧科研 PASS 没有变成当前动作许可；发布判定未宣布训练、在轨抓取或硬件动作已经发生。

本审阅只评价声明与证据边界，不对尚未生成的惯性成果、全部电气计算或整个 WP10 包重新签发工程合格结论。最终交付仍需由主流程核对实际生成的回执和文件绑定。
