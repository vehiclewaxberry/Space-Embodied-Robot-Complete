# 执行限制与恢复记录

本记录区分生成失败、读回失败及最终可交付状态。详细命令、时间、内存阈值见本轮 logs/*.run.json。1400 MiB 子任务限制与512 MiB系统可用内存下限没有放宽，CAD/OCC/COM由root串行执行。

1. 整星 service STEP在完成701实例载入后，导出阶段触发内存保护（logs/whole_service.run.json）。本轮不交付整星STEP；共享舱局部STEP已生成，完整整星使用SolidWorks总装及其本机依赖。
2. 首批原生件任务在C003保存后中断；后续精确冷读C003并完成几何回导比较。原B1设置快照未能在中断前持久化，因此不能证明该批导入前的用户偏好已恢复。results/NATIVE_B1_RECOVERY.json保留这一限制。后续批次先持久化设置，再在结束时恢复；没有关闭无关或用户未保存文档。
3. 初次C019电源线束原生回导的体积积分差略高于1e-5 mm³，而双向材料差为0。随后将积分精度从1e-7细化至1e-9和1e-11，差收敛至约1.29e-10 mm³，精细链差约3.04e-8 mm³。接受阈值未放宽。原HOLD比较保留于NATIVE_MATERIAL_CHECK.json，最终结果为NATIVE_MATERIAL_CHECK_V2.json。
4. 服务态原生总装已经保存，但第一次全量冷读被360秒时间保护中断。NATIVE_SERVICE.json保留原检查点状态；独立恢复只重开同一哈希文件，未重建总装，实际核对701组件/1082实体后完成。最终以NATIVE_SERVICE_RECOVERY.json为准。
5. 停放态第一次入口脚本误以为WP08最终回执均采用NATIVE_STATE.json名称，断言在打开/修改总装前失败。读取冻结WP08交付清单后，明确绑定其NATIVE_PARKING_RECOVERY_V2.json及独立检查。失败日志未覆盖。V4及后续入口沿清单核验回执与父装配哈希。
6. 释放态入口V5将重复的编辑前全量元数据读取改为：冻结父总装哈希、已绑定WP08冷读与独立回执，以及当前完整组件身份集合。最终冷重开仍核验701组件的文件、哈希、完整矩阵和固定状态，并实读82个新实体及其四点坐标变换。停放/释放旧1000实体的体数来自哈希绑定的WP08证据，不冒充本轮再测。
7. 合盖服务棱柱检查真实结果为HOLD：前维护盖有15842.56960512 mm³交叠。条件分析只从原始BRep配对结果移除这个具名盖板，再得出“盖已移除时其余受检件不挡该棱柱”。没有把原HOLD改为PASS，没有将条件分析当作真实工具或拆卸动作验证。
8. 停放态V4在保存成功、冷读到416组件时达到600秒时间限制。后续只重开同一已保存哈希，不重建。最初两次恢复入口分别被“仅允许一个文档”和“先关闭仍被总装引用的零件”断言拦住；现场只有任务自身的干净C010零件与停放总装。V3恢复先恢复已持久化的CommandInProgress设置，再关闭精确路径/哈希验证的总装及遗留零件后重开；无关文档不在关闭许可集合内。最终恢复状态独立记录于NATIVE_PARKING_RECOVERY_V3.json。

以上均不修改历史WP08/WP09文件或科学裁决。最终状态只认本目录results/DELIVERY_STATUS.json中绑定的实际完成回执；RUNNING、HOLD和中断记录作为过程证据保留。
