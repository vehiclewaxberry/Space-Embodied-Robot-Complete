# R2 机械—动力学—控制第二增量闭环裁决（2026-08-27）

## 1. 总裁决

本轮继续执行“机械准入线 + 动力学/控制线，单点汇合”的闭环，但没有伪造完整抓取仿真准入。新增证据目前支持四项加性结论：

1. 150 个 M01 活动物体中，9 个非基座 B601 物体形成全关节域保守刚性运动上界候选；本批新增系统运动证书为 0。
2. C01–C09 构型到几何的 STEP-first 执行矩阵已闭合；现行构型可生成 STEP 仍为 0/9，只有非现行 D01 固定-q0 诊断配方就绪且未运行。
3. 任务空间 characteristic length、参考质量/惯量和 6R 质量加权 DLS 已统一到无量纲空间；结果只适用于当前哈希绑定静态 probe，不是时域控制 PASS。
4. 22 kg / 150 kg 合成目标的抓取后空间惯量组合与动量初始化内核已通过；C08/C09 当前系统实例仍因缺少权威 `T_E_T` 而 `NOT_EVALUATED`。

因此当前允许：继续离线动力学、控制预开发、M01 合同和真实输入准备。当前不允许：CAD/STEP 运行、系统碰撞与路径搜索、真实接触/附着、捕获后 current plant、SAFE/Sim13 non-ABORT、父 Gate 重发或发布。

## 2. M4 C01–C09 构型—几何执行矩阵

机器包：

```text
20_engineering/MECHANICAL_ENGINEERING_RELEASE_R2/_work/
m4_l01_l02_configuration_geometry_execution_matrix_v1/
```

验证结果：Gate 30/30、独立验证 67/67、负控 36/36、pytest 14/14、源锁 37/37。

现行状态：

```text
C01–C09 current STEP buildable     0 / 9
complete integrated geometry       0 / 9
fresh CAD run authorized           false
CAD kernel loaded                  false
STEP generated                     false
snapshot executed                  false
```

唯一可执行准备增量为 `D01_R2_SOLAR_B601_FIXED_Q0_DIAGNOSTIC`：

```text
leaf solids       9 master + 6 Solar R2 + 388 B601 = 403
groups            3
bbox min (mm)     [-230.25, -715.4, -274.86072587989787]
bbox max (mm)     [488.533214, 715.4, 285.63229786565915]
M3R datum (mm)    210.405
```

D01 不是 C01 current：C01 的 `q_home` 与完整 B601 单体 BREP 的固定 `q0` 不同，禁止互相代用。C02–C04 缺失故障帆板变换；C05 缺失经裁决的 stow q 与支撑接触；C06–C07 缺失 ratified task/pregrasp q、夹爪和帆板状态；C08–C09 还缺目标附着、接触界面和受控目标几何。

## 3. M01 刚性随动候选批

机器包：

```text
20_engineering/MECHANICAL_ENGINEERING_RELEASE_R2/_work/odr/
ODR60_OPTION_A_M01_RIGID_KINEMATIC_MOTION_CERT_BATCH_V1/
```

验证结果：Gate 28/28、9/9 候选独立重算、核心负控 44/44、release 负控 10/10、pytest 34/34。

候选上界采用：

```text
L_candidate[i,k] = ceil_1e-9_mm(
    full_locus_surface_radius[i]
    + sum(norm(downstream joint-origin translations))
)
```

两个指爪的完整 0–0.0715 m 行程包络只计入一次；10 个 A 对象 registration 均已验证为有限合法 SE(3)，当前冻结设计资产的 link-frame registration 为 identity。

权限分账保持：

```text
design-screening candidate bounds            9
new system motion certificates               0
parent V4 snapshot                            0 / 150
observed append-only base certificate         1 / 150
remaining without observed certificate        149
scene values / stage instances                0/30 / 0/3
clearance policy / pair queries               0/11166 / 0/11166
continuous edges / path                       0 / false
```

非阻断 P2：冻结注册同时保存精确 URDF 父系指爪轴和理想化纯 ±Y 位移公式；全行程最大差约 `2.626e-7 m`。这不破坏本批 71.5 mm 标量保守包络，但 operational promotion 前必须令公式等于 `axis_parent × state`，或删除重复公式并以轴为唯一规范。

## 4. 无量纲任务空间度量候选

机器包：

```text
30_simulation/r2_control_engineering_closure/task_space_metric_candidate_v1/
30_simulation/r2_control_engineering_closure/task_space_metric_external_audit_v1/
```

冻结尺度：

```text
L_char = 1.170431204685893 m
m_ref  = 31.022864807342987 kg
I_ref  = m_ref L_char^2 = 42.498508062024065 kg m^2
```

定义：

```text
y_dot_bar = W_task y_dot
M_bar     = S_q^-T M_6R S_q^-1 / I_ref
J_bar     = W_task J_6R S_q^-1
A_bar     = J_bar M_bar^-1 J_bar^T + lambda^2 I
qdot      = S_q^-1 M_bar^-1 J_bar^T A_bar^-1 y_dot_bar
```

当前静态 probe：

| 任务 | rank / nullity | 最小无量纲奇异值 | 加权任务残差 |
|---|---:|---:|---:|
| FAR_APPROACH_5D | 5 / 1 | 0.07421114171073408 | 3.5779e-12 1/s |
| FINAL_ALIGNMENT_6D | 6 / 0 | 0.04547587129888624 | 3.6850e-12 1/s |

米↔毫米任务行换元、kg↔g 质量单位换元、0.5×/1×/2× 长度敏感性和非单位 `S_q` 通式均纳入审计。该归一化是明确的工程 convention，不是控制最优性证明；未执行 backend advance、时域跟踪、碰撞、M01、硬件限幅或控制命令。

候选包结果为 Gate 17/17、候选负控 29/29、standalone 12/12、pytest 11/11；最终包外审计为 14/14、负控 40/40、pytest 3/3。外审文件清单采用严格集合全等：缺失 `README.md` 与加入未声明文件均 fail-closed。真实 kg→g 检验在内存中把 16 个 link mass 和 96 个 inertia 分量乘以 1000，并用不同 XML 重新装配质量矩阵；`M_g` 相对 `1000 M_kg` 的误差为 `7.3053e-16`。解析单位消去的零误差与物理重装配的浮点绝对诊断 `1.4551915228366852e-11` 分账记录，禁止互相替代。

## 5. 抓取后空间惯量内核

机器包：

```text
30_simulation/r2_dynamics_engineering_closure/
post_capture_spatial_inertia_kernel_candidate_v1/
post_capture_spatial_inertia_kernel_external_audit_v1/
```

候选结果：Gate 22/22、主负控 30/30、standalone internal 34/34 + 29/29、pytest 95/95；包外审计 18/18、pytest 21/21。外审与更高层汇合必须同时固定 source lock 和 auditor，避免审计脚本与锁共同自签。

两个 synthetic fixture 的服务体质量为 31 kg：

| fixture | 组合质量 | 空间惯量交叉最大差 | 线动量残差 | 关于 O 的角动量残差 |
|---|---:|---:|---:|---:|
| C08 / 22 kg | 53 kg | 7.11e-15 kg m² | 2.22e-16 kg m/s | 1.11e-15 kg m²/s |
| C09 / 150 kg | 181 kg | 4.26e-14 kg m² | 2.22e-15 kg m/s | 2.31e-14 kg m²/s |

角动量参考点已显式输入为 `origin_O_in_I_m`：

```text
omega = I_C^-1 [H_O - (r_C - r_O) × P]
```

参考点平移律和反叉乘符号 falsifier 均通过。公共 API 拒绝 NaN/Inf、非正质量、非对称/非正定或违反主惯量三角不等式的惯量。`admit_current_system_attachment()` 不接受调用者注入 contract；它内部加载 canonical contract，并实际重算 receipt 与 configuration-state 文件 bytes/SHA、frame ID 和构型记录 hash。

当前合同没有 `attachment_authority_receipt` pin，因此：

```text
C08 = NOT_EVALUATED_MISSING_AUTHORITATIVE_ATTACHMENT_SE3
C09 = NOT_EVALUATED_MISSING_AUTHORITATIVE_ATTACHMENT_SE3
```

合成内核不是接触、锁闩、冲量分配、非零总动量时域 plant 或动能守恒证明。

## 6. CAD 运行准入

2026-08-27 07:02:53（JST）只读观测：

```text
available physical memory              2.4175987243652344 GiB
6 GiB admission gate                   false
R2_DP_RUN_ID                            null
R2_DP_RUN_AUTH                          null
ACCEPT_SINGLE_RUN_LOW_MEMORY_RISK       null
```

因此本轮没有调用 CAD 内核。Option A Owner 分支选择已记录，但不是 named CAD run authority；旧授权和旧低内存 override 均不可复用。

## 7. V2 加性汇合

机器包：

```text
20_engineering/MECHANICAL_ENGINEERING_RELEASE_R2/_work/
current_r2_mechanical_dynamics_control_increment_v2/
```

最终机器裁决：

```text
technical_verdict       PASS_ADDITIVE_MECHANICAL_DYNAMICS_CONTROL_INCREMENT_V2__NO_PARENT_REISSUE_OR_DOWNSTREAM_AUTHORITY
Gate                     38 / 38
upstream bytes/SHA pins  27 / 27
standalone validation    7 / 7
negative controls        28 / 28
pytest                    13 / 13
package inventory        8 files; cache/bytecode 0
review_status             PENDING_OWNER_REVIEW
```

27 个直接上游锁包含任务度量外审的 source lock、auditor、tests、README 和 receipt 五件套。构建器与独立验证器均通过 byte-identity deterministic replay；validator 不导入 builder。最终核心 SHA-256：

```text
Gate         EB077DCCC022B440C93D2911C61EFE206D87FF1917FC08D322A955BC62F67B3A
Manifest     3084F37751985B358821105141B55C36FEBC099ABDABB66F58C6361F6435CE64
Receipt      8039B27BB35326C649CE5215D0FCF63C5C979DFBA2ACAA120542C1B36B66F94A
Source lock  7449CA17A071D05851846DFDAE96F24794C6231C856A781DFBAD2C961E0DA63E
```

该包只做 append-only 证据汇合：`parent_gate_reissued=false`、`parent_gate_credit=false`、`next_stage_authorized=false`、`release_credit=false`，18 项 authority boundary 全为 false。最终红队裁决为 P0=0、P1=0、P2=1；P2 是 V2 八件套尚未被更高层 parent/release ledger 外部锚定，在当前有界增量范围内不阻断，但下次父级重发必须直接锁定本包最终字节。

## 8. 下一闭环顺序

1. 仅在新的 named CAD run 权限与当时 6 GiB 内存门同时满足，或用户给出与该 run 绑定的 `ACCEPT_SINGLE_RUN_LOW_MEMORY_RISK` 后，执行 D01/C01 所允许的 STEP-first 构建。
2. 对生成 STEP 做实体数、组、bbox、M3R datum 和四视图复核；固定-q0 D01 不得冒充 C01 `q_home`。
3. 补 C01–C09 的 articulatable per-link BREP、夹爪值、帆板/HDRM/latch、C08/C09 目标与附着权威，形成 current collision assets。
4. 绑定 M01 30 个场景值、3 个阶段实例、剩余 149 个 motion certificates、11166 clearance/pair、连续边和 bounded path。
5. 用实测夹爪闭合时间、接触刚度/阻尼、目标质量属性、权威 `T_E_T` 和锁闩状态建立 current post-capture plant。
6. 再执行时域 Control、SAFE 独审、Sim13 system binding 和 non-ABORT；最后才可重发父 Dynamics/Control/Joint Gate。

## 9. 当前最终边界

```text
offline mechanical/dynamics/control predevelopment     CONTINUE
M01 candidate-bound preparation                        PASS_ADDITIVE_ONLY
current configuration CAD/STEP                         HOLD
system collision / pair / edge / path                   HOLD
current C08/C09 post-capture plant                      NOT_EVALUATED
time-domain control / SAFE / Sim13 non-ABORT            HOLD
parent Gate reissue / next stage / release              false
```
