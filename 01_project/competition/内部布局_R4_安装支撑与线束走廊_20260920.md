# 内部布局 R4：安装支撑与线束走廊

日期：2026-09-20。用户授权按既有规划继续数字候选设计；硬件尚未采购。

本轮已把接口板四点支撑、20 件安装五金和两处功能线束调整写入独立 R4 CAD，并生成可由本机 SolidWorks 打开的原生总装。R1–R3 保持原封存状态。

主目录：`20_engineering/SERVICE_STAR_INTERNAL_LAYOUT_R4_20260920/`。

- 原生总装：`native/SERVICE_STAR_SERVICE_R4.SLDASM`。1130 个叶实例、1540 个实体实例、697 个唯一零件文件、14 个直接子装配；含既有功能包络与候选件。
- 安装改动：四个 3.4 mm 贯穿孔，4 个 5 mm 高支撑柱，4 个 M3×16 名义螺栓、8 个垫圈和4 个螺母；均待实际型号、材料等级与预紧确认。
- 名义间隙：接口到夹具 0.50→2.00 mm；释放路线到 M3RB 0.75→2.865439 mm；最近其他障碍 2.625653 mm。原线路端点保持不变，R14 曲率、切向连续性及解析体积核验完成。
- 三个固定状态共 363 对有效增量实体检查，无检出的未声明新增穿透。制造公差和连续运动未获验收信用。
- 独立复验：`PASS_WITH_OBSERVATIONS`，1037/1037 检查通过。发现的尺寸合同未接入缺陷已修复，15 个字段扰动与真实构建入口负控均拒绝生成，原合同逐字节恢复。
- 原生冷复验：0 打开错误、0 警告，全部实例解析并固定，总变换最大元素误差6.66e-16，强制重建成功、NeedsRebuild2=0，758 个文件在冷复验前后哈希一致。

查看 `views/R4_INTERNAL_LAYOUT.png` 或 `views/R4_INTERNAL_LAYOUT_VIEWER.html` 可检查实际几何。浏览器已打开 `http://127.0.0.1:8766/R4_INTERNAL_LAYOUT_VIEWER.html`。整星原生截图为 `views/SERVICE_STAR_SERVICE_R4_RESOLVED.png`。BOM 的6 行替换和20 行新增见 `inputs/BOM_DELTA.csv`。

CF1 电源/热控模块仍未装入。完整 V30 来源声明56个组件实例，STEP 冷读为60个实体出现项；全部实体均参与搜索。18个旋转/平移候选全部发生冲突。最佳候选在最终 R4 的服务、停放、释放状态各有14项碰撞，包含P60托架、轮组和线束支撑。体积汇总混合实体和功能包络，是逐配对值之和，不能作为唯一物理干涉体积。该结果仅限所测网格，不能推断全部空间不可布置。

接口板底面与原TIM仍隔4.5mm，板到机壳的导热、电隔离和接地尚未设计闭合。后续先处理当前V36 MAIN板的紧凑布局及CF1热接口，再补接口板导热路径、实物线束端接与完整材料/质量账本。具体输入、动作和验收条件已写入 `inputs/NEXT_LAYOUT_WORK_ORDER.json`。

本次为本地数字布局增量，不是整星全部系统设计完成、可上电或可在轨实装的结论。原生总装仍依赖R1的同级native目录；R4 ZIP不是Pack and Go便携整包。
