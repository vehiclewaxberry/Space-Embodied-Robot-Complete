# R2 动力学与控制工程闭环阶段裁决（2026-08-27）

## 1. 总裁决

本轮已完成 `R2_DYNAMICS_AND_CONTROL_ENGINEERING_CLOSURE` 的**执行型合同、诊断实现与联合 fail-closed Gate**，但没有达到 Dynamics、Control 或联合系统 PASS：

```text
ODR-60 Option A Owner branch selection     RECORDED
Mechanical static preflight                HOLD
R2 dynamics engineering                    HOLD
R2 control engineering                     HOLD
SAFE independent review                    HOLD
Sim13 current non-ABORT binding             HOLD
M01 bounded path search                    NOT EXECUTED
Joint system readiness                     false
next_stage_authorized                      false
release_credit                             false
```

这不是“工作没有开展”，而是已把此前容易误判为 PASS 的量纲、守恒独立性和授权语义问题转化为明确机器阻断项。当前合法最高结论为：

```text
HASH_BOUND_ENGINEERING_ARTIFACTS_BUILT
INDEPENDENT_RED_TEAM_P0_P1_ZERO
ENGINEERING_HOLDS_RETAINED_FAIL_CLOSED
NO_PATH_SEARCH
NO_PHYSICS_GATED_EMBODIED_INTELLIGENCE_RELEASE
```

## 2. 机械准入线

### 2.1 Owner 选择

附件第十一节“直接交给 Claude Code/Fable5 的执行提示词”之后紧邻 fenced code block 的原始首个非空行，已被记录为：

```text
AUTHORIZE_OPTION_A_FIXED_ENDPOINT_M01_TRAJECTORY_SEARCH
```

授权证据类固定为：

```text
EXPLICIT_EXECUTION_PROMPT_BLOCK_FIRST_LINE
```

解析器对推荐段、普通引用、非首行 token、多分支 token 均 fail-closed。`ACCEPT_SINGLE_RUN_LOW_MEMORY_RISK` 未被授权；`run_id` 与授权时间窗均为 `null`。

### 2.2 已闭合范围

- execution mount 的 full-precision spelling 已绑定；
- accepted B601 URDF 子树 10/10 条静态 frame relation 已逐项交叉核验；
- M01 `PRE_RELEASE / RELEASE_EVENT / POST_RELEASE` 三阶段 schema 已建立；
- 旧 V1 重复键 Gate 保持历史原件，新 V2 strict JSON 作为当前消费入口；重复保留解析证明 `next_stage_authorized` 与 `release_credit` 各出现两次，四个原始值均为 native `false`。

### 2.3 未闭合范围

```text
operational narrowphase asset              1 / 10
three-stage scene instances                0 / 3
clearance policy                           0 / 11,166
motion certificates                        0 / 150
pair oracle execution                      0 / 11,166
continuous edge certificates               0
geometry query                             0
path search                                0
```

因此 Option A 只是研究分支选择，尚未形成几何、pair、edge 或 path 权限；机械 handoff 仍因 G12 harness operational envelope 为 FAIL。

## 3. R2 约束动力学线

已建立 19-link、18-joint、6R+2P、总质量 `31.022864807342987 kg` 的 hash-bound CURRENT_R2 诊断，并显式实现：

```text
M(q) ddq + b(q,dq) = tau + J^T reaction
J ddq = 0
```

关键可复算结果：

| 项目 | 结果 | 信用边界 |
|---|---:|---|
| 2P 锁止反力 | `[+0.0071996810, -0.0088089393] N` | KKT 组件诊断 |
| KKT/消元 6R 加速度差 | `2.22e-16 rad/s²` | 分单位交叉 |
| KKT/消元 2P 加速度差 | `0 m/s²` | 分单位交叉 |
| KKT/消元反力差 | `8.67e-19 N` | 分单位交叉 |
| 基座线加速度交叉差 | `5.42e-20 m/s²` | 分单位交叉 |
| 基座角加速度交叉差 | `1.73e-18 rad/s²` | 分单位交叉 |
| `tauP=0` 自由模式 `ddqP` | `0.40850956 m/s²` | 证明零力不等于锁止 |
| 0.004 s 能量漂移 | `3.77e-15` | 短时数值 smoke，无守恒信用 |
| 机械联接线/角动量残差 | `3.52e-18 Ns / 3.64e-18 Nms` | 代数零动量恒等式，无独立守恒信用 |

红队处置后，约束动力学 Gate 为 `16/18`，两项硬 HOLD 为：

1. `DG1_heterogeneous_coordinate_metric_bound=false`：6R 的 rad 与 2P 的 m 尚无冻结的无量纲参考度量；原始 8×8 质量矩阵谱与条件数只保留为 `raw_unscaled_*_no_credit`。
2. `DG2_independent_full_state_momentum_conservation=false`：当前每个采样点由机械联接重建基座速度，未独立积分 base pose/twist，也未验证非零总动量案例。

Solar R2 双翼 14 模态 LOW/NOMINAL/HIGH 自由环降仍为组件诊断；`eta` 与 `deta` 的求解器交叉已分开，不再混合不同量纲。臂—翼全时域耦合、接触混杂切换和 TARGET_ATTACHED plant 均未实现。执行器 152/152 个实测字段仍为 `null`，没有零填充。

## 4. R2 控制线

已完成 CTRL-01 负结果因果账本、6R+2P 任务合同、5D/6D 广义雅可比原始诊断、flex/supervisor 接口与总控制 Gate。以下事实被保留：

- B601 物理主链为 6R，没有虚构第七转动关节；
- 5D 结构 nullity 为 1，6D 为 0；
- `qP=[0.03575, 0.03575] m`、`qdotP=0` 只是静态诊断状态，不是动态锁止证明；
- CTRL-01 仍为 `REPEAT`，当前树精确回放为 HOLD；
- 所有 backend advance、collision、contact、pair/edge 与 M01 path 调用均为 0。
- 5D 的两条角任务已按“工具接近轴在正交平面内的轴率”实现为 `a_dot = omega × a`，不再把任意两项角速度直接冒充轴率；6D 仍使用完整工具角速度。
- CG4 与 CG7 的 candidate 布尔现分别由执行器/轮组阈值和当前 SAFE replay/运行授权原生派生，当前均为 `false`，不再硬编码为真。

红队发现控制器把 m/s 与 rad/s 直接拼接做 SVD、欧氏残差和 DLS，缺少 characteristic length / task-space weighting matrix。不同但物理等价的单位缩放可改变 `sigma_min` 并翻转守卫。因此：

```text
task_space_metric.status                       UNBOUND
command_emitted                                false
CG1 candidate_packaging_pass                   false
control_engineering_complete                   false
```

原始 5D/6D 数值仍可复现，但只作为 `RAW_MIXED_UNIT_DIAGNOSTIC_NO_CREDIT`。其中 generalized 解虽将原始混合单位残差降至约 `1.30565e-10 / 1.34478e-10`，预测基座角速度却分别是 naive 的 `3.30549× / 3.29410×`；“最小基座扰动”没有成立。

## 5. 联合 Gate

`R2_DYNAMICS_CONTROL_SYSTEM_GATE_V1` 已直接哈希绑定 19/19 个机械、动力学、控制、SAFE 与 Sim13 来源，并继续逐行核验七类上游闭合清单：机械包 `10/10`、机械源 `22/22`、动力学源 `27/27`、动力学包 `12/12`、动力学 SHA `13/13`、控制源 `24/24`、控制 SHA `14/14`。独立红队凭据也已被 Gate 与包清单绑定，末审 `P0/P1=0`。当前 13 个联合就绪条件仅 4 个为真：

```text
candidate_integration_contract_bound             true
source_binding_pass                              true
upstream_manifest_integrity_pass                 true
owner_option_a_selection_recorded                true
independent_red_team_high_findings_zero          true
mechanical_handoff_pass                          false
dynamics_engineering_pass                        false
control_engineering_pass                         false
safe_independent_review_pass                     false
sim13_binding_pass                               false
joint_system_ready                               false
ready_for_physics_gated_embodied_intelligence    false
path_search_executed                             false
unknown_auto_allow_count                         0
next_stage_authorized                            false
release_credit                                   false
```

独立审查凭据 SHA-256 为 `37172A0F9200A387E39434579C3FAE3CE9F2CAC17BC7A51DC3A7C3B2AC41E4EA`；凭据固定映射到当前联合合同、builder 与测试文件，交换 ID/路径、绝对路径、目录穿越、额外 ID、布尔冒充整数和非规范哈希均由负测拒绝。

SAFE-00 的历史机器 Gate 为 PASS，但 `review_status=PENDING_REVIEW` 且 `next_stage_authorized=false`；Sim13 的 20/20 是负控后端信用，最大运行态仍为 `ABORT_ONLY`，不能升级为抓取执行权限。

## 6. 下一闭环顺序

1. 冻结 6R/2P 无量纲参考尺度或广义坐标参考度量，并加入 m↔mm 重参数化不变性试验。
2. 独立积分基座位置、姿态、twist 与关节状态；加入非零总动量案例，分别审计线/角动量。
3. 冻结控制任务空间 characteristic length 或物理权重矩阵，再重做 5D/6D singularity、DLS 与残差 Gate。
4. 完成 M01 三阶段实例、活动对象/pair universe、clearance、motion、oracle 与连续边证书。
5. 仅在静态预检全 PASS 后进行运行前内存测量；低于 6 GiB 时针对明确 `run_id` 另行请求单次 override。
6. 执行一次 bounded M01 几何搜索；若成功，最高只授予 `VERIFIED_GEOMETRIC_PATH_CANDIDATE_PENDING_TIME_AND_OWNER_RELEASE`。
7. 完成路径时间参数化、臂—翼全时域耦合、硬件执行器、接触/捕获后 plant 与 SAFE 独审，再重跑联合 Gate。

## 7. 配置与资产保护

本轮没有修改 accepted B601 URDF、Unified R2 URDF、CAD/STEP/mesh、E23/Round4、M3R、Solar R2 或历史负结果。由于没有可见几何变更，CAD snapshot review 与 URDF viewer handoff 不适用。

## 8. 独立验证摘要

```text
ODR-60 mechanical execution closure          22/22
R2 dynamics engineering                      15/15
R2 control engineering                       13/13
R2 joint system Gate                          7/7
current authority index                       7/7
E23 protected recertification                 7/7
total                                        71/71
unit/uncertainty static audit                 0 findings / 2 source files
independent joint red-team                    P0/P1 = 0
```

受保护来源哈希复核：

```text
accepted B601 URDF   1BC2B7483CD8025D08BA6EADFD9E1F3B0477121714E7CF4DDC1794D9E471C164
Unified R2 URDF      D84AA23CE98A2A9C697B1F32E433C01C3F0AE3BD0B5C56EF9A218DD88911CBDA
CTRL-02 Gate         240FA708B53646A024BB0FD2D67085188DD7C81685515B69C4CDC5DC50EDFC5E
sim_05 B601 model    3E2B451476F437E482661E275073249AD101705251B78C042851C32AE741052F
```

## 9. 同日后续加性证据收口（现行增量）

本节晚于上文父 Gate 的生成时间，只记录新增候选，不追写历史机器裁决。上文第 6 节的前两项缺口已分别由独立 DG1/DG2 候选补证：6R+2P 参考度量完成单位重参数化不变性验证；规定 8-DOF 运动下完成非零惯性系总动量、基座位置/四元数传播与 16 个惯性 link 逐体复算。它们仍不是扭矩驱动 plant。

随后形成三条新的边界化候选：

- DG3：冻结线性切点、C=0 三角点动量、无强迫能量/动量、真正 modes-removed、质量矩阵 Cholesky 正定布尔审计；混合单位 28×28 谱量无 Gate 信用，E23 数值不继承；
- DG4：22/150 kg、恢复系数 LOW/NOMINAL/HIGH、0.005 m/s 的两个独立刚体冲量诊断；终态仅为 `POST_IMPACT_DIAGNOSTIC`，不存在 attached/captured 权威；
- DG5：1024 个 `estimate ±1u` 确定性设计角点，其中 880 个仅为基本刚体约束未被证伪、144 个三角不等式失败并隔离；不作概率或 as-built 声明。

当前统一增量裁决：

```text
CURRENT_R2_DIGITAL_PROTOTYPE_DYNAMICS_ENTRY_GATE_V3
19/19
PASS_ADDITIVE_DG1_TO_DG5_DESIGN_CANDIDATE_CONVERGENCE__
PARENT_DYNAMICS_CAD_M01_ROUTE_C_PHYSICAL_CONTACT_NON_ABORT_CONTROL_AND_RELEASE_HOLD
```

ODR-60 Option A、execution mount full-precision spelling 和 accepted URDF 子树 10/10 frame ledger 已由较新记录闭合；旧控制/绑定 intake 中“Option A 未选”和“mount/frame 未绑定”只在对应字段上被 supersede。M01 required fields 仍为 `0/9`，三阶段实例仍为 `0/3`，clearance/motion/oracle 仍为 `0/11166`、`0/150`、`0/11166`，查询与搜索均为 0。

因此现行合法工作范围升级为“DG1--DG5 候选证据消费＋离线控制预开发”，没有升级为父 Dynamics 或 Control PASS。联合 Gate 仍是 4/13；Mechanical handoff、SAFE 当前树独审、Sim13 system binding、non-ABORT、RL/VLA、hardware/flight execution 与 release credit 均保持 false。

接下来的单点汇合顺序不变：命名 run 的内存准入/单次 Owner override → 完整 CAD 候选生成与审查 → M01 场景、对象/配对、clearance/motion/oracle/连续边 → bounded path → 时变扭矩驱动 plant、捕获后重构、任务度量控制与 SAFE → 重发父 Dynamics/Control/Joint Gate。

## 10. 机械单点汇合的执行准备增量（V4）

本节不重发父 Dynamics/Control/Joint Gate，只登记机械入口新增证据。

- CAD 生成后验证准备：11/11，专项测试 24/24，独立红队 P0/P1/P2 = 0/0/0；候选 STEP/GLB/snapshot 仍未生成。
- M01 场景/碰撞预绑定：18/18，测试 17/17；30/30 决策键显式但权威值 0/30，场景 0/3；167 行资产账本仅 1/150 具资产级 operational authority；pair query 0/11166，path search false。
- R2 当前态 append-only 重发：27/27 源、Gate 28/28、独立 validator 22/22、pytest 31/31；父文件零修改，Route-C/Sim13/材料与清单债务均保持原边界。
- V4 汇合：28/28，Gate SHA-256 `277BE6349EC87E3CC76519E824F368F1D720C2C7E01646255DA5865510CB87EF`，manifest SHA-256 `A1A6BBC5222400423F6523FD3443371B5A2087D330777DDE89918519E4F2316B`。

2026-08-27 04:45:28（JST）生成器同口径可用内存为 `0.9722824096679688 GiB`，低于 6 GiB；当前没有 `R2_DP_*` 命名 run/override 环境、活动写锁或候选输出。因此机械线现在处于“执行准备通过、CAD 未运行”的状态，不得把 V4 PASS 解释为机械 handoff、M01、非 ABORT 或发布 PASS。

当前双线汇合顺序进一步具体化为：新命名 CAD run 授权与内存准入 → 399-solid STEP/GLB → topology/逐实体/bounds/安装基准后验 → 四视图审查 → M01 三实例与对象/pair universe → 11166 clearance/oracle、150 motion、连续边与 bounded path → 时变扭矩和捕获后 plant → task-space 度量控制、SAFE、Sim13 → 父 Gate 重发。
