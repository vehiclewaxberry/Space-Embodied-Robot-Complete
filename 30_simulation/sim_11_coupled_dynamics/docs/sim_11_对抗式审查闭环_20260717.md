# sim_11 对抗式审查闭环 — 2026-07-17

## 1. 原后台工作流的真实完成度

原工作流设计为 5 个主审维度（physics / capture / spec / code / report），每条发现再交给
2 名反驳者核实，理论任务数为 5 个主审 + 最多 30 个反驳任务。

实际日志只收到：

- 4/5 个主审结果：physics、spec、code、report；capture 主审未返回；
- 11/30 个反驳结果；
- 因后处理规则要求“同一发现至少 2 名反驳者都无法反驳”才进入 `confirmed`，
  在反驳任务大量缺失时输出的 `confirmed=[]` 不能解释为“没有问题”。

因此本次不沿用原工作流的空结论，而是逐条回到代码、参数卡、结果 JSON/CSV 和报告做根审。

## 2. 根审闭环

| 编号 | 审查发现 | 处理 |
|---|---|---|
| R1 | G3a 实际是独立 FFR 帆板组件对 sim_07a 固支根部工件，不是自由漂浮整星逐点等价 | 在 Gate JSON、报告和 CLAUDE.md 明示“组件级退化证据范围”；整星锁关节路径由 A2、G1、G5 与回归测试覆盖 |
| R2 | G3b 轨迹和 sim_07 阻尼存在重复字面量 | G3b 从 A1 场景卡读取轨迹；阻尼从 `sim_07a_task_response.ZETA` 读取 |
| R3 | G2/G4 只覆盖 A1，未覆盖真正模态活跃的 A2 | A2 捕获后无阻尼能量审计加入 G2；A2 m=2/3/4、rtol 扫掠加入 G4 |
| R4 | A2 参数卡 `base_scene` 被 loader 读取后未被执行路径消费 | `run_scene_a2` 按 `base_scene_path` 调用 A1；新增级联测试 |
| R5 | A1 的 `max_base_CoM_shift_m` 把基座原点误称为质心 | 改为 `max_base_origin_shift_m` |
| R6 | A2 图中体组曲线按系统质心计算，参考线却用惯性原点角动量 | JSON 同时输出 `H_total_about_inertial_origin_Nms` 与 `L_total_about_com_Nms`；图统一使用质心口径 |
| R7 | 3DOF 点捕获把未约束角速度差写成总体 `rel_post_max`，容易误读为求解残差 | 拆分为已约束自由度残差与未约束角速度差 |
| R8 | `0.5‖Δη̇‖²` 被写成“注入模态能”，漏掉 A1 残余模态速度交叉项 | 同时输出“速度跃变能量尺度”和“实际模态动能变化” |
| R9 | 报告 full 漂移、92× 比值、测试数与最终证据不一致 | 改为机器工件支持的 2.6e-12/1.4e-12、92.8×（FFR）/92.4×（ANCF）、22 项测试 |
| R10 | 报告引用了 A2 Radau/BDF 模态能量差，但 G5 原字段未纳入该量 | 将 `A2_modal_E_peak` 加入 G5 机器裁决，最大差 2.16e-8，仍通过 5% 门槛 |
| R11 | A1 先写摘要、后画图，JSON 未记录已经生成的 PNG | 画图后回写摘要；名义与刚化摘要均补入可追溯的 `png` 路径 |
| R12 | A2 参数卡列出捕获接口、抱闸/并入开关与翻滚轴约定，但执行路径未硬校验 | 装载 `capture_interface` SSOT，并对约束模式、`sim_common` 轴约定、抱闸和目标并入契约做失败即停校验 |

## 3. 补证后的机器结论

- 前置基线：sim_05 全量测试 22/22 PASS；sim_07 无独立 `tests/` 目录，按其 README
  复跑 ANCF benchmark 与 `--smoke`，五项 benchmark、R1、R3 均 PASS，冻结目录无变更；
- G1 动量守恒：PASS；
- G2 能量审计：A1 无阻尼 4.0e-13，A2 捕获后无阻尼 5.63e-14，PASS；
- G3 退化链：三个子项 PASS，但 G3a 明确限定为帆板组件级证据；
- G4 收敛性：**FAIL**。A2 基座峰值和 tip 峰值通过 1% 门槛，但模态能量峰值
  m2→m3 变化 2.78%、m3→m4 变化 1.27%；
- G5 Radau vs BDF：PASS，A2 最大相对差约 2.16e-8。

最终裁决必须写为 `SIM11_GATES_FAIL: G4_convergence`，并保持
`next_stage_authorized=false`。旧的 `SIM11_GATES_PASS_WITH_PROVISIONAL_PARAMS`
只对应审查前、未覆盖 A2 收敛性的旧口径，不再有效。

## 4. 下一步门槛

当前不应通过改阈值或删除模态能量指标把 G4“跑绿”。应先选择并记录一种物理修复：

1. 提高名义模态截断阶数，继续做相邻阶次收敛；
2. 若真实捕获不是理想瞬时冲量，引入有来源的接触持续时间/柔顺性，对高频输入做物理带宽约束；
3. 杨恒数值案例到位后，以真实模态频率、振型和阻尼替换占位参数，再重跑全套 Gate。
