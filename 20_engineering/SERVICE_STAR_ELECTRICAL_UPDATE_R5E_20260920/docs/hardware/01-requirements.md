# 服务星 R5E｜2026-09-20｜阶段1：沿用约束

本轮按用户“先数字设计与候选选型、尚未采购”“连接服务星主机并预留板位”的既有授权推进，不重新选择整套电源或主控架构。

目标是地面工程样机。B601 驱动数量/版本和星载接口仍引用 R3 输入；本轮没有以自制驱动替换 B601。MAIN、独立 AUX、STOP 与制动回馈路径分别保留。

沿用 `POWER_CHAIN_SELECTION.json` 中 RRC3570-4 / CHB500W-24S24N / THN30-2415WIR 候选。24V、12V、3.3V 是电路名义轨，不是实测全温供电边界。20A 是既有主路筛查工况，不是此次批准的持续工作电流。

电气主源是与实板匹配的249-ref/795-pin-net网表，SHA256 `90497ad91d4b8ec2c39e6eda819b94e3fbcf9f908861aae22e6c90c5bf1a1939`。以物理PCB和源锁判定，不按文件夹名推断新旧。

实际负载波形、K1完整线圈规格、再生能量、连接器端接、控制板真实发热、热接触与辐射边界、线束制造长度仍缺输入。原R2 `ELECTRICAL_ACCEPTANCE_INPUTS.json`保持有效，不填写假设测量值。正式采购时再核实时效性的库存、交期与生命周期。
