# Geometry SSOT v1 — arm mount adapter (decision D-1 baseline interface)
version: v1
footprint_mm: [160, 160, 12]
boss: {diameter_mm: 100, height_mm: 15}
mass_kg: 1.2
cg_m: [0.0, 0.0, -0.01726]        # adapter local frame (JSON)
frame_M: see frame_tree_v1.yaml (T_SM nominal_frozen_v1)
native_part: 20_engineering/cad/spacecraft_layout/servicer_6U_v0/robot_mount_adapter_v0.SLDPRT.SLDPRT
  # valid modern-SW container; rename+relocate pending (D-9 hygiene item)
deprecated_interfaces:
  - {footprint_mm: [140, 140, 15], where: 12U spec flange primitive, action: update to 160 in skeleton_v1}
  - {footprint_mm: [90, 180], holes: 8, where: 6U requirements doc, action: historical, not consumed}
