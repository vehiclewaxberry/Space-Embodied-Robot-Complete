# End Effector Boundary

> `STATUS: REFERENCE_ONLY`  
> `PHYSICAL_TCP: UNKNOWN_DISABLED`

B601 参考装配中保留 `gripper_link`、`gripper_left` 和 `gripper_right` 的包络代理，用于识别末端区域。

本阶段没有建立：

- 物理接触 TCP
- 指端接触面
- 刚性锁定接口
- 力/触觉传感器
- 合格的抓取区域

虚拟任务 frame `E` 不等于 `G`，也不等于 physical TCP。
