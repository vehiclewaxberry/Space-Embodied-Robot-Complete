# B5.1R1 S01_CR01 修复执行准备状态

S02_SR01 已成功附着并只读打开 Master Skeleton，但在第一个重建读回周期发现 `G1B_RECEIPT_SHA256` 不存在。SolidWorks 正常关闭，零保存，全部受保护文件未改变。

已完成：

- 追加式失败事件及最小五属性范围更正；
- `S01_CR01` 修复工具源码与 DLL；
- 两个独立 Agent 的最终静态 PASS；
- 原 V2、Stage A、旧 Gate 和全部失败证据继续冻结；
- 新 R1 与 `WORK_S01_CR01` 均尚未创建；SolidWorks 进程数为 0。

工具只允许从原 V2 创建字节一致的隔离工作副本，并仅保存新文件：

```text
02_MASTER_SKELETON/B51R1_MASTER_SKELETON_V2_R1.SLDPRT
```

修复范围严格限定为五个缺失属性。写入后验证全部 24 项文档属性、21 项原生合同特征、4 项辅助特征、44 项总库存、3 个构型、零实体和零外部引用。

下一步必须取得所有者的新授权；当前 S02_SR01 槽已消费，不能外延为 CAD 写入或再次冷重开。

建议一次性授权措辞：

> 已重新打开 SolidWorks，并授权新增一个 S01_CR01 原生属性修复槽；仅允许从现有 V2 创建 B51R1_MASTER_SKELETON_V2_R1.SLDPRT、使用 Add3 修复五项缺失文档级文本属性并保存新文件，严禁覆盖或删除 V2、Stage A 与既有证据。同时授权 S01_CR01 通过后新增一个 S02_R1 冷重开验证槽：仅允许启动一个新的可见 SolidWorks 进程，对 R1 只读验证并正常退出，禁止保存和强制终止。总并发仍为 1，任一失败立即 fail-closed，不进入 S03。
