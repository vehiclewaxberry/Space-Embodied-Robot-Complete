# Builder failure supersession record

`builder_failure.log` is retained as historical evidence of a failed final hashing step while SOLIDWORKS still held document handles.

It is superseded for the final V1.0 state by:

1. `native_build.log`
2. `native_component_manifest.csv` with 32/32 current hashes
3. `native_inspection.json` with 247/247 PASS
4. `inventory_validation.json` with overall PASS
5. raw and annotated review-view hash manifests
6. `evidence_seal_manifest.csv`

This supersession does not erase the failure or authorize a clean-first-pass claim.

