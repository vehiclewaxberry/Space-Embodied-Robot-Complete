# 2026-09-21 公开发布前的路径重命名记录

本轮为把仓库推送到 GitHub 而做的路径重命名。**只改路径，不改字节**；各文件内容与原件逐字节一致，无内容被丢弃。

## 原因

Windows 保留设备名（`AUX` `CON` `PRN` `NUL` `COM1`–`COM9` `LPT1`–`LPT9`）出现在路径的**任何一段**时，该路径可被目录枚举列出，但无法被 Win32 `open()` 打开。Git for Windows 走 Win32 API，因此 `git add` 在这些路径上以 `exit 128` 硬失败：

```
error: open("...): No such file or directory
error: unable to index file '...'
fatal: adding files failed
```

MSYS2 层（Git Bash 的 `head`/`mv` 等）能正常读取同一文件，因此**内容本身完好**，问题只在 Win32 路径解析。重命名是保留内容的唯一可行方案；排除这些文件会丢失 R7 的 CAD 源。

## 重命名清单

| 原路径 | 新路径 | 条目数 |
|---|---|---|
| `20_engineering/SERVICE_STAR_AUX_STOP_INSTALLATION_R7_20260921/cad/source/AUX/` | `.../cad/source/AUX_BOARD/` | 目录 + 14 个 `AUX_000.step`–`AUX_013.step` |
| `01_project/.../wp10_mechatronic_closure_20260908/implementation/results/aux_v34/aux.dsn` | `.../aux_v34/aux_board.dsn` | 1（17,815 B） |
| `01_project/.../wp10_mechatronic_closure_20260908/implementation/results/aux_v35/aux.dsn` | `.../aux_v35/aux_board.dsn` | 1（34,985 B） |

说明：
- `cad/source/` 下另一子目录 `STOP/` 不是保留名，未改动。
- `AUX_000.step`–`AUX_013.step` 的**文件名未改**（`AUX_000` 带后缀数字，不是保留名）；只改了其父目录名。
- 被重命名文件的**内容未作任何修改**，文件大小与修改时间保持原值。

## 已知的陈旧引用

- `20_engineering/SERVICE_STAR_AUX_STOP_INSTALLATION_R7_20260921/results/extract_boards.log` 中记录的是抽取时的原路径 `cad/source/AUX/`。该日志是历史执行记录，**按其原记录时间理解**，不随本次重命名改写。
- 全树检索未发现其它功能性路径引用（配置、脚本、清单均无命中）。

## 同轮的其它发布前修改

- `70_tools/runtime_wp09_kicad/`（KiCad 10.0.6 便携运行时，约 1.4 GB，含 922.9 MB 安装包）已加入 `.gitignore`。该安装包超过 GitHub 单文件 100 MB 硬限，且属第三方可再分发运行时，非本项目产出。请从 https://www.kicad.org/ 自行安装。
- `01_project/.../wp09_interfaces_20260907_1525/system_completion/mass/r01_sources/wuerth_{nut_M3,screw_M3x10,screw_M3x12,washer_M3}.html` 中伍尔特网站自带的 Google 浏览器端 API key 已就地替换为 `REDACTED_THIRD_PARTY_BROWSER_KEY`。该 key 属第三方公开前端 key，非本项目凭据；打码是为避免公开转发他方密钥。四个页面的其余内容未改动，质量溯源链完整。

## 对科学结论的影响

**无。** 本轮只改路径与一处第三方密钥打码，不触及任何模型、参数、结果或机器裁决 JSON。Gate 数值、布尔值、字段结构与裁决文本均保持原语义。不得据本记录升级或改写任何工程或科学结论。
