# Assembly Phase — State Truth & Scope（Agent A / Gate AG-A0 前置）— 2026-07-20

HEAD **738348e** 干净。磁盘裁决：SAFE-00 **PASS**(47/47)、CTRL-02 **PASS**、
CTRL-01 **REPEAT**（CP6 方案 B 收口：3 缺陷已修复、7 项真实负结果预注册保留）、
sim_10/11/12 PASS 家族、Wave1 `WAVE1_REPEAT` 存档合并（dce4c08，非 PASS 声明）。

## ALREADY_COMPLETE（装配规划不得重做/重命名冒用）
自由漂浮刚柔世界模型(sim_11)+接触带宽(contact_window)、可行域(sim_10)、
策略选择(sim_12)、SAFE-00 安全门、CTRL-02 分账控制、动量账本方法学、
威胁登记 W1-R12/R13、Physics Tool 合约草案(10_research/vla/)。

## 装配阶段可复用（经最新 Gate 核验后）
- sim_11 J*/FFR/接触带宽 → ASM-01/02 的动力学核心（只读 import）
- SAFE-00 合同与裁决核 → ASM 各阶段授权层（扩装配 reason_code 属新增不改核）
- CTRL-02 轮/推力器分账 → 装配中基座稳定（W1-R12 轮力矩 0.033>0.01 PROVISIONAL
  冲突必须在 ASM-02 显式解决——**接口级阻塞候选**）
- CTRL-01 负结果 → 直接约束装配控制设计：严格 6D 零空间为 0（LOCK 段允许）、
  5D 接近段才有 1 维零空间、冻结增益闭环锚超差=装配须相位切换而非单一 6D 任务

## 科学边界声明（/goal 指令原文纪律）
不得假设"通用末端跟踪已解决"；CTRL-01 负结果是装配分阶段语义的证据基础。
禁止声明：已完成自主在轨组装/自由漂浮实物验证/VLA 飞行级控制/固定基座≡微重力/
CTRL-01 已普遍解决轨迹控制。

## 主场景冻结（PI 指令）
12U 服务星 + B601 将 1U/2U 可更换载荷模块安装到**预制接口**（锥面/导向销粗对准
→插销孔精对准→机械锁扣→电源/数据状态位模拟），目标星带双柔性帆板，双方自由
漂浮；地面演示=固定基座 B601+数字自由漂浮模型。大桁架=后续扩展不入本阶段。
技能词表：OBSERVE/GRASP_MODULE/MOVE_TO_PREASSEMBLY/APPROACH_5D/ALIGN/
COMPLIANT_INSERT/LOCK_6D/VERIFY_ASSEMBLY/RETREAT。

## 外部依赖（继承）
帆板参数卡（杨恒）、B601 夹爪 T_c 与执行器实测（触发 W1-R12/R13 重跑）；
新增：装配接口公差/刚度/锁紧力无任何实测——**全部 PROVISIONAL/LITERATURE**。

**Gate AG-A0 = PASS**：无状态冲突；所有结论有磁盘证据路径。
