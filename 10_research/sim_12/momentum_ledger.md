# sim_12 Gate 0 — Momentum Ledger（机器复算版）— 2026-07-18

> 数字来源：冻结求解器只读复算（git 2d26ddc 工作区干净；`build_bodies` +
> `rigidize`，守恒残差 ≤4e-16）。基准工况：碎片 150 kg@3°/s，λ=1，v_app=0.01。
> 2026-07-18 Reviewer-2 独立复算修订：S2 成本链三处更正（Δv=0.037475 m/s、
> 1.69 g、矢量 |ΔH_vec|=1.43508 与标量 +0.873 分列），S3a 补有效域 θ<60°。
> 账本口径：H 均对系统质心（惯性系）；捕获为内力冲量 ⇒ **H_capture⁻ ≡ H_after
> 恒成立（机器验证）**，策略间差异全部来自 **H_before 的选择** 与 **捕获前后的
> 外部输入**。

## 基准分解（Q3 的答案）

| 项 | α=0（S1） | α=1（S2） |
|---|---|---|
| 目标自旋 I_t·ω_t | 3.67768 | 3.67768 |
| 追踪星轨道项 m_c(r−r_com)×(v−v_com) | 0.19533 | **1.19192** |
| 目标轨道项 | 0.03451 | 0.21057 |
| **\|H_before\|=\|H_after\|**（矢量和） | **3.65099** | **4.52436** |
| \|J\| / ω⁺ / ΔT | 0.33846 / 3.06333 / 5.27 mJ | 0.19388 / 2.94700 / 1.96 mJ |

**Q3**：速度匹配让末端跟随抓点 ⇒ 追踪星平动与目标自旋**同旋向** ⇒ 轨道项
×6.1（0.195→1.192），矢量和 +24%；同时相对速度 ↓ ⇒ 冲量 −43%、耗散 −63%。
J 与 H 是不同的物理量：J 由相对速度决定，H 由绝对运动状态决定——不矛盾，不违反守恒。

## 逐策略账本（Q1/Q2）

| 策略 | ΔH_internal | ΔH_external（来源/时机） | H_after vs S1 | 成本行 |
|---|---|---|---|---|
| S1 被动 | 0 | 0 | 基准 3.65099 | — |
| S2 速度匹配 | 0 | **矢量角冲量 \|ΔH_vec\|=1.43508 N·m·s**（= m_c(r_c−r_com)×v_match，接近段推力器建立 Δv=\|ω_t×r_g\|=**0.037475 m/s**，捕获前；\|H\| 标量净变 +0.873 为另一口径，勿混用） | +24% | ΔV 推进剂 m_c·Δv/(Isp·g0) = **1.69 g**（冷气 60 s） |
| S3a 轮组预置 | 0（轮-体交换） | 预置期推力器卸载 ±h_max（捕获前） | 不变 | **3.00 g**；收益=可用动量交换区间（球心）平移：\|h_rw0+ΔH\|≤h_max，沿预测方向吸收上限 = 2h_max·cosθ（θ=0 时 0.3→**0.6 N·m·s**；θ=30° 严格 **−13.4%**；**有效域 θ<60°，θ=60° 收益归零、更大反而劣于不预置**；方向不确定大时满偏置未必最优——部分偏置留作 16 例敏感性子项） |
| S3b 臂摆整形 | **0（构造性：内部运动不改总量）** | 0 | 不变（仅瞬态分布/接触点速度改变） | 关节功耗；**禁止宣称改善终态 H** |
| S3c 推力器辅助 | 0 | ∫r×F_ext dt（捕获前/后均可） | 按预算 | 与 S4 共享 54.73 g ⇒ H 消化上限 5.47 N·m·s@l_T=0.17 |
| S4 捕获后消旋 | 0 | ∫r×F_ext dt（捕获后） | — | sim_08 口径 |

**Q1**：无一策略在捕获瞬间改变总 H；能改变系统总 H 的只有外部力矩（推力器），
S2/S3a 的"改变"发生在**捕获前对初态的选择**，其动量来源（推进剂）已入账闭合。
**Q2**：臂=纯内部（0）；接近段推进=外部（S2 的 +0.873）；轮组=内部交换
（容量区间语义）；推力器=唯一外部净输入。
**Q4**：是——S3a 严格为容量区间平移，无动量创造（上表口径）。
**Q5**：成立——S1 省预算但 J/ΔT 大（结构门 binding 时劣），S2 护结构但动量门
变差 24%（资源门 binding 时劣）：**binding-gate dependent strategy selection**
是被数字支撑的判据框架。

## Gate 0 机器断言清单（进入 16 例前必须编码为 assert）
1. 每策略 |H_capture⁻ − H_after| ≤ 1e-12（内力冲量恒等；注意该恒等式半构造性，
   独立性由惯性原点口径残差承担）；
2. S3b 终态 H 与 S1 逐位一致（**预注册断言**：冻结二体求解器中臂为刚性 stack，
   须在 sim_11 多体路径上成为机器事实后方可宣称"构造性证明"）；
3. S2 的**矢量**外部角冲量 ΔH_vec = m_c(r_c−r_com)×v_match 与 H(α=1)−H(α=0)
   闭合（|ΔH_vec|=1.43508 N·m·s，基准工况）；|H| 标量差 +0.873 只作展示量；
4. 成本行（1.69 g / 3.00 g）由公式复算非手抄。
措辞纪律：ΔT 只是柔性能量**上界/一致性检查**（capture_impulse.py 自注），
"S2 护结构"类表述必须以冲量/载荷口径陈述，不得以 dT 直接背书；
"推进剂已入账"指质量账与外部角冲量已定量，排气动量逐项账留 Phase 1 声明。
